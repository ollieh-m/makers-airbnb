<!DOCTYPE html>
<html>
	<head>
		<title>Makers AirBnB</title>
	</head>
	<body>
		<h1>Makers AirBnB</h1>
	      	<% if current_user %>
		      	<form id="signout" class="navbar-form navbar-right" action="/sessions" method="post">          
					<input type="hidden" name="_method" value="delete">
			        <button type="submit" class="btn btn-primary">Sign out</button>
		       	</form>
	      	<% end %>  
		    </div>
		  </div>
		</nav>
		<%= partial :flash %>
		<% if current_user %>
			<h1 style="text-align: center;">Welcome, <%= current_user.name %></h1> 
		<% end %>
		<%= yield %>
	</body>
</html>